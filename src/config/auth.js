/**
 * Configuration de l'authentification
 * Le mot de passe devrait idéalement être stocké dans un fichier .env
 * mais pour des raisons de simplicité, on le stocke ici
 */
export const AUTH_CONFIG = {
  // Mot de passe de 12 caractères pour l'accès administrateur
  adminPassword: "pingpingping12",
}
